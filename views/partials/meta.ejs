<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Home Page</title>
  </head>
  <body style="background-color: #2b2b2b; color: #f2f2f2">
    <style>
      h1 {
        text-transform: capitalize;
        font-size: 2rem;
      }
      a {
        color: rgb(118, 162, 233);
        text-decoration: none;
      }
      nav {
        width: 100%;
        position: fixed;
        top: 0;
        left: 0;
        display: flex;
        justify-content: space-between;
        align-items: center;
        background-color: #1c1c1c;
      }
      nav h1 {
        margin-left: 3rem;
      }
      nav ul {
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        width: 25%;
        list-style: none;
      }

      #jobApplicationForm {
        margin: 8rem auto 0 auto;
        background-color: #181818;
        padding: 3rem;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        width: 100%;
        max-width: 500px;
      }

      #jobApplicationForm label {
        display: block;
        margin-bottom: 8px;
        font-weight: bold;
      }

      #jobApplicationForm input,
      #jobApplicationForm textarea,
      #jobApplicationForm select,
      #jobApplicationForm button {
        width: 100%;
        padding: 10px;
        margin-bottom: 20px;
        border: 1px solid #ddd;
        border-radius: 4px;
      }

      #jobApplicationForm textarea {
        resize: vertical;
        height: 100px;
      }

      #jobApplicationForm button {
        background-color: #28a745;
        color: #fff;
        border: none;
        cursor: pointer;
      }

      #jobApplicationForm button:hover {
        background-color: #218838;
      }
    </style>
  </body>
</html>
